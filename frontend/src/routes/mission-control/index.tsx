import { createFileRoute } from '@tanstack/react-router'; import { PipelineNetworkMap } from '../../components/mission-control/PipelineNetworkMap'; import { blockOnProductionHost } from '../../lib/internalOnlyGuard'; export const Route = createFileRoute('/mission-control/')({ beforeLoad: blockOnProductionHost, component: () => <PipelineNetworkMap /> });
